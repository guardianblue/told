# The TokenRule class is used by Tokenizer class to determine how to tokenize a given program code
class TokenRule

  attr_accessor :name, :regex

  # Initialize a TokenRule object
  def initialize(name, regex)
    @name = name
    @regex = regex
  end

end
